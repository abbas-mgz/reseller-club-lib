{
  "name": "reseller-club-lib",
  "version": "1.4.8",
  "description": "A Node.js library for interacting with the Reseller Club API, enabling easy domain management and account operations.",
  "main": "dist/index.js",
  "scripts": {
    "build": "babel src --out-dir dist --presets=@babel/preset-env",
    "test": "jest",
    "prepublishOnly": "npm run build",
    "release:patch": "npm run build && npm version patch && npm publish",
    "release:minor": "npm run build && npm version minor && npm publish",
    "release:major": "npm run build && npm version major && npm publish"
  },
  "presets": [
    "@babel/preset-env"
  ],
  "dependencies": {
    "axios": "^1.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.27.0",
    "@babel/core": "^7.26.10",
    "@babel/preset-env": "^7.26.9",
    "babel-preset-env": "^1.7.0"
  },
  "peerDependencies": {
    "axios": "^1.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/abbas-mgz/reseller-club-lib.git"
  },
  "license": "MIT",
  "keywords": [
    "resellerclub",
    "api",
    "domain",
    "nodejs",
    "library"
  ],
  "author": "Abbas Marghaei",
  "bugs": {
    "url": "https://github.com/abbas-mgz/reseller-club-lib/issues"
  },
  "homepage": "https://github.com/abbas-mgz/reseller-club-lib#readme"
  ,
  "files": [
    "dist",
    "README.md"
  ]
}
